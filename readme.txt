1. You need some packages installed: django and channels_redis,daphne
and already have docker and daphne runtimes installed on Mac

2. To use asgi in this djano project:
- shell command 'source run_docker' to use redis
- shell command 'source run_daphne' or 'python manage.py runserver' to run a web server
